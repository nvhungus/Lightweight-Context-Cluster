experiment:
  name: resnet18_cifar
data:
  name: cifar10
  root: data
  download: true
  batch_size: 128
  val_batch_size: 256
  workers: 2
  augment: true
model:
  name: resnet18_cifar
  num_classes: 10
train:
  epochs: 200
  lr: 0.001
  weight_decay: 0.05
  label_smoothing: 0.1
  amp: true
